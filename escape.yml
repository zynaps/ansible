---
- name: escape setup
  hosts: escape
  become: yes

  roles:
    - router
    - common_packages
    - vnstat
    - wireguard
    - docker
    - node_exporter
    - docker_registry
    - postfix
    - redis
    - rutor_watch
    - nginx
    - feeder
