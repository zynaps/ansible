---
- name: bootstrap host
  hosts: all
  become: yes

  roles:
    - apt_update
    - disable_ipv6
    - sshd_tweak
    - common_packages
    - vnstat
    - wireguard
    - docker
    - docker_registry
    - users
